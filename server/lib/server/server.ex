defmodule Server.Game do
  import Enum

  # Get a new, blank game object
  def new() do
    %{}
  end

  # The view
  def view(state, username) do
    %{}
  end

  # TODO: Contains server-side functionality, this will probably be moved to a different file, if so change the alias in lib/server_web/channels/room_channel.ex

end
